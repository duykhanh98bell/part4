@model projectPart3.Models.Gia

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (Session["tenKH"] != null)
{
    <h2>Delete</h2>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Gia</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.gia_goc)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.gia_goc)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.gia_khuyen_mai)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.gia_khuyen_mai)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ngay_ap_dung)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ngay_ap_dung)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>

}
else
{
<h1>Bạn cần đăng nhập để tiếp tục</h1>
}
